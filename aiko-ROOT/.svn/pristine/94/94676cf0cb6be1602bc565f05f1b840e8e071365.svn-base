package com.ycb.mapper;

import java.util.List;
import java.util.Map;

import org.apache.ibatis.annotations.Param;

import com.ycb.domain.restart_message;

public interface restart_messageMapper {
    int insert(restart_message record);

    int insertSelective(restart_message record);
    
	 List<restart_message> getTtimeList(@Param("params") Map<String, Object> params);
}